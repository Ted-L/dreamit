export declare function resolve(specifier: string, fromFile: string, extensions?: string[]): {
    type: 'found';
    result: {
        type: 'real';
        filename: string;
    };
} | {
    type: 'not_found';
    err: Error;
};
